"""Task scheduling dashboard backend.

The package import is intentionally side-effect free.  Import ``app.main`` to
construct the HTTP application, or import ``app.services`` in worker processes.
"""
