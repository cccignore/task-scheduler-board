# 设计与实现说明 —— 任务调度看板

> 本文回答三个问题：**题目要了什么、我们怎么设计的、具体怎么做的**。
> 一页速览见 [README.md](README.md)；参数演变的逐 key 推演见 [PARAMETER_EVOLUTION.md](PARAMETER_EVOLUTION.md)；协作与 AI 使用披露见 [COLLAB.md](COLLAB.md)。

---

## 0. 题目要求 ↔ 实现对照总表

| 题目硬性要求 | 我们的实现 | 关键代码 | 证据 |
|---|---|---|---|
| 1. 三层参数合并（L1 base / L2 组覆盖开工生效 / L3 粘性覆盖 + 空串保持语义） | 纯函数解析 + 启动事务内冻结 L2 快照、一次性算出全部 Step 生效参数并落库 | `app/services.py` 的 `resolve_parameter_chain`、`start_task` | `tests/test_parameters.py` 7 个测试（含随机链对照独立 oracle）；`PARAMETER_EVOLUTION.md` |
| 2. 并发安全认领（任意时刻一个任务只被一个 worker 持有） | SQLite 写锁事务 + 条件更新 + 服务端签发凭证（claim token）+ 未开工任务的开工租约回收（已开工仅手动重派，避免副作用重复执行） | `app/services.py` 的 `claim_next_task`、`_reclaim_expired_leases` | `scripts/run_concurrency_proof.py`：8 真实进程 × 40 轮 = 320 次同抢，0 重复；页面「多进程证明」按钮可现场复跑 |
| 3. 幂等执行日志（重复上报只留一条、不可被矛盾覆盖） | 数据库唯一约束兜底 + 先查后插 + 首写生效；矛盾上报写入操作台账留痕 | `app/services.py` 的 `complete_step` | `scripts/run_idempotency_proof.py`：5 真实进程同报 → 1 写入 + 4 no-op；页面「并发完成 ×5」现场演示 |
| 4. 极简状态看板（展示状态 + 并发×5 入口 + 实时刷新） | 五列流水线看板 + 任务详情弹层 + ×5 验证面板 + 系统操作台账，1.5s 轮询；模拟 worker、多进程证明、清空重置全部页面可操作 | `app/static/` 三件套 | `evidence/dashboard-browser-proof.txt`（真实 Chromium 验收） |
| 技术栈说明（为什么选它、并发测试如何保真） | Python + FastAPI + SQLite(WAL)；证明用 `multiprocessing` spawn 真进程 + 每进程独立连接 | 见 §2、§4.4 | README、本文档 |
| SQLite → PG/MySQL 抽查点 | 迁移方案写明：`SELECT … FOR UPDATE SKIP LOCKED` + 条件更新，不变量不变 | 见 §4.5 | README |

---

## 1. 我们对题目的理解

题目本质是一个**微型分布式作业调度器**：多个不受控的 worker 进程（可能同机、可能跨机）从同一个队列里抢任务、按序执行步骤、汇报结果。三个硬性需求分别对应分布式系统里的三个经典问题：

1. **配置的分层与生效时点**（参数合并）——考"语义想没想清楚"；
2. **互斥与所有权**（并发认领）——考"并发原语用没用对，测试是不是真并发"；
3. **至多一次记录**（幂等日志）——考"重试风暴下的数据一致性"。

题目还明确了规模（≤10 worker、≤5 任务/秒）并提示"数据库自身机制就够用"——所以我们的设计基调是：**用最少的活动部件，把不变量交给数据库守护，用真实多进程测试证明它**，而不是引入消息队列/分布式锁中间件。

---

## 2. 总体架构

```
浏览器看板 (原生 JS，1.5s 轮询)
      │  HTTP/JSON
FastAPI 薄 API 层 (app/main.py，异常→状态码映射)
      │  函数调用
服务层 (app/services.py)  ←—— 所有业务不变量都在这一层的事务里
      │  每次操作独立连接
SQLite (WAL 模式，app/db.py)
      ▲  各自独立连接
真实 worker 进程 × N (app/worker.py)
   ├─ 页面托管：POST /api/workers/managed → multiprocessing spawn (app/worker_manager.py)
   └─ 命令行：scripts/run_worker.py
```

设计上的几个"故意"：

- **服务层不持久化任何内存状态**，每个公开操作开独立连接、显式事务。这让"API 进程"和"worker 进程"在数据库面前完全对等——worker 不必经过 HTTP，直接调服务层函数即可，这正是题目设定的部署形态。
- **薄 API 层**：`main.py` 只做参数校验（Pydantic）、异常翻译（404/409/422/503）和静态文件挂载，没有任何业务逻辑，保证"指着任何一行业务代码提问"时答案都在 `services.py`。
- **看板是纯观察者 + 演示触发器**：前端不做任何业务判断，所有状态都来自后端返回；连"幂等验证是否通过"也是拿服务端响应和数据库行数核对出来的。

### 数据模型（5 张表）

- `groups`：组及其 L2 覆盖参数；
- `tasks`：任务主状态机（pending/claimed/running/done/failed）+ 认领三元组（claimed_by / claim_token / lease_expires_at）+ L2 快照列；CHECK 约束强制"非 pending 必有持有人和凭证"；
- `steps`：有序步骤（UNIQUE(task_id, sequence)），带各自的 L3 覆盖和启动时算好的 `resolved_parameters`；
- `execution_logs`：幂等账本，**UNIQUE(task_id, step_sequence) 是整个需求 3 的最终防线**；
- `operation_logs`：跨进程操作台账（认领/开工/上报/重复忽略/矛盾/回收/重置），页面"系统操作台账"面板直接读它。

### 任务状态机

```
pending ──claim──▶ claimed ──start──▶ running ──最后一步成功──▶ done
   ▲                  │                  │ └──任一步失败──▶ failed（终态）
   ├──开工租约过期自动回收（未开工，无副作用，安全）──┘
   └──操作员手动重派（已开工卡死时，人确认副作用可重放）──┘
```

Step 级：pending → running →（done | failed），同一时刻至多一个 Step 处于 running；上一步成功才推进下一步。

---

## 3. 硬性需求 1：三层参数合并

### 3.1 规则的形式化

记 `B` 为 L1 基础参数，`G*` 为**首次成功 start 时**读到并冻结的 L2 组参数，`O_i` 为第 i 个 Step 的 L3 覆盖，`E_i` 为第 i 个 Step 实际拿到的参数：

```
E_0 = shallow_merge(B, G*)          # L2 空串是普通字面值，会真实覆盖
E_i = copy(E_(i-1))；对 O_i 中每个 (k, v)：
        v 是精确的 JSON 空串 ""  → 跳过（保持当前值；原本没有就继续没有）
        否则                    → E_i[k] = v（覆盖，并粘性传给后续）
```

几个容易做错、我们明确定了的边界：

- **只有 L3 的精确 `""` 有保持语义**；`0`、`false`、`null`、`{}`、`[]`、`" "`（空格）都是正常值，照常覆盖——防止实现里用"truthy 判断"踩坑；
- **空串对不存在的 key 不会凭空创建它**（"保持当前值"的当前值不存在，就继续不存在）；
- **浅合并**：嵌套对象整体替换，不递归合并；由此推论**嵌套对象内部的 `""` 只是数据**，不触发保持语义；
- **L2 的生效时点是"首次 start 的事务瞬间"**：start 前改组对任务可见，start 后改组不影响已冻结快照；持有者重试 start 幂等地复用已存快照；崩溃恢复后新 worker 续跑也复用（见 §4.3），保证"开工时一次性生效"在任何时序下都成立。

### 3.2 实现方式

`resolve_parameter_chain` 是一个**纯函数**（输入三层参数，输出每个 Step 的独立深拷贝快照），便于穷举测试；`start_task` 在一个写事务里完成"读组 → 冻结 `G*` → 算出全部 `E_i` → 落库到 `steps.resolved_parameters`"。选择**启动时一次性预计算并持久化**而不是执行时现算，理由：

1. L2 冻结时点要求本来就锚定在 start 事务上；
2. L3 覆盖在任务创建时就已固定（没有运行中改 Step 的 API）；
3. 落库后的快照是可审计的——看板上"某一步当时到底用了什么参数"直接读库，不存在"重算出另一个结果"的可能。

### 3.3 怎么证明它是对的

- 边界矩阵测试逐 key 断言 Step 1→5 的完整演变（覆盖/保持/新 key/假值/嵌套替换全组合）；
- **随机链对照测试**：200 组随机 L1/L2/L3 输入，与一个独立实现的参照模型（"某 key 当前值 = 截至本步最后一次非空覆盖，否则初始合并值"）逐快照比对——两套实现殊途同归，防"实现和测试是同一个人写的同一种错"；
- 快照独立性测试：改一个快照不影响另一个（深拷贝验证）。

---

## 4. 硬性需求 2：并发安全认领

### 4.1 问题分析

"同一任务任意时刻只被一个 worker 持有"要防三种失效：

1. **抢单竞态**：两个 worker 同时读到同一个 pending 任务、同时改状态；
2. **冒名顶替**：认领只记 worker_id 的话，任何人报相同 id 就能操作别人的任务；
3. **持有者消失**：worker 拿到任务后崩溃，任务永久卡死（这不是题目硬性要求，但是这套语义唯一"运行久了必然坏"的洞，我们补上了）。

### 4.2 抢单：把互斥交给 SQLite 的单写者模型

```sql
BEGIN IMMEDIATE;                     -- 立刻取得整库唯一写锁
SELECT id FROM tasks WHERE status='pending' ... ORDER BY created_at, id LIMIT 1;
UPDATE tasks SET status='claimed', claimed_by=?, claim_token=?, lease_expires_at=?
 WHERE id=? AND status='pending' AND claimed_by IS NULL AND claim_token IS NULL;
COMMIT;
```

- `BEGIN IMMEDIATE` 使"读候选 + 改状态"整体互斥：写锁到提交才释放，其他连接在此期间根本无法开始自己的认领写事务，**竞态窗口为零**；
- 条件 UPDATE 的 `rowcount == 1` 是第二道不变量校验（防御性，正常永远成立）；
- 表上的 CHECK 约束（非 pending 必有持有人 + 凭证）是第三道；
- 认领详情的多表读放在**写事务提交之后**的独立读事务里，缩短写锁持有时间。

### 4.3 所有权：服务端凭证 + 开工租约 + 手动重派

- **凭证**：认领成功时服务端生成 `secrets.token_urlsafe(32)`，只在认领响应里返回一次；之后 start / complete 必须同时给出 worker_id + token，用 `secrets.compare_digest` 常数时间比对。任务列表接口不回传 token，所以"知道任务 id + 编个 worker_id"无法接管任务。
- **租约是"开工时限"，不是"执行时限"**：认领即授租（默认 900s，`TASKBOARD_LEASE_SECONDS` 可调）。**自动回收只针对"认领后一直未 start"的任务**，发生在下一次 claim-next 的同一个写事务里：任务回到 pending、凭证清空、新认领签发新 token，旧 worker 的迟到请求全部 409。这样收窄的原因是安全性可以严格论证——claim 与 start 之间 worker 尚未执行任何 Step，**没有外部副作用，回收不可能造成重复执行**；而回收与 start 都是写事务，串行化保证二者只有一个成功（start 赢则任务 running 不再可回收，回收赢则 start 因 token 轮换 409）。
- **已开工（running）任务绝不自动重派**：这是一次显式的设计修正。凭证轮换是数据库层的 fencing——它能拒绝旧 worker 的**上报**，但撤销不了旧 worker 已经执行的**外部动作**（比如消息已经发出去了）。若自动回收 running 任务，旧 worker 可能仍在执行某个 Step 的副作用，新 worker 重新执行同一 Step，同一条消息就发了两遍——违反"任何时刻唯一持有"的实质语义。生产级解法需要 worker 心跳 + 执行前 fencing 检查 + 外部副作用幂等化（见 §9），超出题目规模。
- **手动重派（操作员决策）**：卡死的 running 任务通过 `POST /api/tasks/{id}/requeue`（看板任务详情里的按钮，带二次确认与风险提示）显式回收——**只有人能判断"外部副作用是否可以安全重放"**，系统不能替人做这个决定。重派后凭证作废、正在执行的 Step 回退，已完成 Step 的状态与执行日志保留。
- **断点续跑**：重派后的新认领者 start 时复用首次冻结的 `G*`（参数语义不漂移），并从第一个仍是 pending 的 Step 继续——幂等账本天然就是进度表。

### 4.4 怎么证明"测试是真实的并发"

题目明确排斥语言层伪并发（`Promise.all` / `asyncio.gather`）。`scripts/run_concurrency_proof.py` 的设计：

- `multiprocessing` **spawn** 模式启动 8 个独立 OS 进程（不是 fork、不是线程），每进程**预先建立并自验证**自己的数据库连接（`PRAGMA database_list` 核对文件路径）；
- 每轮只投放**一个** pending 任务，三道屏障（ready/start/finished）让 8 个进程在同一瞬间发起认领——制造最坏情况的碰撞，而不是靠量大碰运气；
- 赛后逐轮核对：恰好 1 个赢家、赢家与预期任务一致、DB 终态（claimed 数、pending=0）、每个凭证唯一且与 DB 一致；任何子进程报错、退出码非零、结果缺失都判 FAIL；
- 结论：40 轮 × 8 进程 = 320 次同时认领，重复 0 次。页面「多进程证明」按钮可在临时数据库上现场复跑同一脚本（快速 12×6 或完整 40×8）。

### 4.5 抽查点：换成 PostgreSQL / MySQL 还成立吗

成立，但**不能照搬 `BEGIN IMMEDIATE`**（那是 SQLite 的单写者语法）。迁移方案：

```sql
BEGIN;
SELECT id FROM tasks WHERE status='pending'
 ORDER BY created_at, id LIMIT 1 FOR UPDATE SKIP LOCKED;
UPDATE tasks SET ... WHERE id=? AND status='pending';
COMMIT;
```

行级锁 + `SKIP LOCKED` 让多个 worker 并行认领**不同**任务（比 SQLite 吞吐更好），条件更新继续保证状态迁移原子性；凭证/租约字段与逻辑照搬；幂等账本仍由同一个唯一约束兜底。**核心不变量（唯一持有、首写生效）不依赖 SQLite 的任何特性**，只依赖"事务 + 行锁/条件更新 + 唯一约束"这三样任何关系库都有的东西。当前 SQLite 版的边界也如实声明：单机共享一个数据库文件；跨机器就应连同一个 PG/MySQL。

---

## 5. 硬性需求 3：幂等执行日志

### 5.1 设计

分层防御，最终防线在数据库：

1. **唯一约束** `UNIQUE(task_id, step_sequence)`——即使代码全错，账本也不可能出现两行；
2. 事务内**先查已有日志**：有则直接返回 `duplicate: true` + 账本原文（首写内容），完全不碰状态；
3. 没有才 `INSERT ... ON CONFLICT DO NOTHING`，且**只有真正插入成功的那一次**驱动状态推进（Step 置 done/failed、任务推进/终止、租约续期）；
4. **首写生效是双向的**：成功后补报失败改不掉成功；失败后补报成功也不能复活已终止的任务。

### 5.2 矛盾上报的处理哲学

对调用方，重复上报永远是"温和的成功"（返回 duplicate + 账本原文），这样 worker 的重试逻辑可以无脑重放；但**矛盾的重复上报**（账本记成功、又有人报失败）是值得运维知道的异常信号——它被写进 `operation_logs`（warning 级）并输出结构化日志，页面台账里"只看告警"一勾就能看到。**幂等契约对外稳定，可观测性对内留痕**，两者不冲突。

### 5.3 怎么证明

- `scripts/run_idempotency_proof.py`：5 个真实 spawn 进程、各自独立连接、屏障同步后**同一瞬间**上报同一 Step → 恰好 1 次写入 + 4 次 no-op、账本 1 行、内容是首写；随后追加双向矛盾重试验证；
- 页面「并发完成 ×5」把同样的实验做成一键演示：5 个并发 HTTP 请求 → 面板核对响应模式 + 数据库行数，4 次 no-op 同时出现在系统台账里；
- 单元测试覆盖越序上报（409）、非持有者上报（409）、伪造凭证（409）等旁路。

---

## 6. 需求 4：状态看板（以及为什么长这样）

题目只要求"极简、能看状态、有 ×5 入口、能刷新"。我们把它做成了**演示与解释工具**，原则是：每个硬性需求都要在页面上"看得见、点得着、可验证"，全程不需要命令行：

- **五列流水线布局**：pending → claimed → running → done/failed，任务卡随轮询在列间流动——状态机本身就是页面结构；首屏的流水线图和三张"白话卡"（配方三层 / 一单一工人 / 只记一笔账）先讲清楚系统在干嘛；
- **任务详情弹层**：L1 / 冻结的 L2 快照 / 当前生效值三栏并排；每道工序的 L3 覆盖用徽章直接标注语义（`覆盖·粘性生效` / `空串·保持当前值`）；底部是题目要求字段齐全的执行日志台账（日志 ID / Step 序号 / 结果 / 时间）；
- **操作台三步走**：① 造任务（演示任务 / 批量排队任务）→ ② 一键上岗**真实独立进程**的模拟工人（可选 15% 失败率演示失败终止；页面显示 PID，全部下班后租约到期任务会被回收重派）→ ③ 也可以手动当工人（认领 → 启动 → ×5）；
- **系统操作台账**：读的是所有进程共写的 `operation_logs` 表（不是前端拼的）——认领、开工、上报、重复忽略、矛盾告警、租约回收、清空，全都有据可查；
- **多进程证明面板**：把两个证明脚本变成按钮，在临时数据库上现场跑真实 spawn 进程并展示统计结果；
- **凭证安全的可视化**：×5 / 启动按钮只在本标签页持有该任务凭证时出现；租约剩余时间在卡片上倒计时。

技术上是零依赖原生 JS + 原生 CSS（设计令牌 + 状态色系统，色彩永远配文字标签）；1.5s 轮询，负载数据 JSON 对比无变化不重渲染。

---

## 7. 关键取舍（答辩高频问题预演）

| 决策 | 我们的选择 | 理由 |
|---|---|---|
| 要不要消息队列 / Redis 锁 | 不要 | 题目规模（≤10 worker、≤5 任务/秒）下数据库事务的串行化损耗完全可接受；少一个中间件就少一类故障与部署成本。题目也明说预期如此 |
| SQLite 还是 PG | SQLite（并写明 PG 迁移法） | 本地零部署即可跑通全部演示与证明；不变量不依赖 SQLite 特性，迁移路径见 §4.5 |
| 租约回收放在哪 | 下一次 claim-next 的事务里，而非后台线程 | 回收只在"有人要任务"时才有意义；不引入常驻调度线程就没有它的崩溃/竞态问题；代价是"没人认领就暂不回收"——对这个语义无害 |
| 回收范围 | 只自动回收未开工（claimed）任务；running 卡死仅手动重派 | token 轮换挡得住数据库写入，挡不住已发出的消息；claimed→start 之间无副作用可严格证明回收安全，running 的重放风险必须由人判断。自动化到 running 需要心跳 + 执行 fencing + 副作用幂等，超出题设规模 |
| 参数何时计算 | start 时全部预计算落库 | L2 冻结时点本就锚定 start；落库快照可审计；resume 幂等重算结果恒等 |
| 重复上报返回什么 | 200 + duplicate 标记 + 账本原文 | 幂等接口对重试方应当是成功语义；矛盾者在台账留痕而不是报错 |
| `/api/demo` 返回 claim_token | 接受（演示专用） | 看板一键演示需要凭证做 ×5；正常任务的 token 仍只在认领响应出现一次。写明这是 demo 取舍 |
| 页面托管的"模拟工人" | 真实 spawn 子进程复用 `app/worker.py` | 和命令行 worker 同一段代码、同样直连数据库——演示即真实，不是前端定时器假装的 |

**刻意砍掉**（题目鼓励写明）：生产级鉴权/RBAC、任务分页、正式 migration 框架（只保留一次性加列迁移）、WebSocket 推送（轮询够用且更简单）、跨机器部署（写明应连同一个 PG）。这些都不影响题目要求的核心不变量。

---

## 8. 测试与证据体系

```
tests/test_parameters.py            参数语义 7 项（含边界矩阵、随机链对照 oracle、嵌套空串）
tests/test_state_and_api.py         状态机顺序、凭证防冒用、一致性读快照、API/看板 smoke
tests/test_lease_and_recovery.py    租约授予/续期/终态清空、过期回收 + token 轮换、断点续跑、活租约不被抢
tests/test_operations_and_console.py 操作台账全生命周期、托管 worker 真进程执行、证明 API、重置
tests/test_multiprocess_proofs.py   两个证明脚本以小参数纳入常规测试 + 进程启动失败清理
scripts/run_concurrency_proof.py    8 进程 × 40 轮同抢：0 重复（页面可一键复跑）
scripts/run_idempotency_proof.py    5 进程同报：1 写入 + 4 no-op + 双向矛盾无效
evidence/                           两个证明的实跑输出 + 真实浏览器验收记录
```

验证口径：一切以数据库终态、响应模式和进程退出码为准，不以 UI 文案为准。当前全量 22 项测试通过；两个证明脚本独立实跑通过。

---

## 9. 已知局限与下一步

- **规模**：全库单写锁在 ≥ 数百任务/秒时会成为瓶颈——届时按 §4.5 迁 PG 并用 `SKIP LOCKED`；任务列表全量返回，需要分页时加游标；
- **安全**：无鉴权，任何能访问端口的人都能操作看板（含清空、起停 worker）；生产需加认证 + 授权 + 审计；
- **worker 契约**：worker 必须持久化自己的 claim token 才能在自身重启后继续上报；认领后未开工崩溃的可等租约自动回收，已开工崩溃的任务会停在 running，需操作员手动重派；
- **running 卡死的自动化恢复**：需要 worker 心跳、每个外部动作执行前的 fencing 校验（携带 token 查询自己是否仍是持有者也只能缩小而非消除窗口）以及外部副作用的幂等设计（如带去重键的发送接口），这是生产系统的正确方向，本题按规模取舍为手动重派；
- **时钟**：租约比较依赖服务端时钟单调合理；NTP 大幅回拨会推迟回收（不会错误抢占，因为回收还要求 lease 非空且确实早于当前时间）；
- **操作台账**：只增不删（重置除外），长期运行需要滚动清理策略。

---

## 10. 关于 AI 协作

本项目由单人 + AI 协作完成：Codex 完成初版实现与对抗审阅，Claude 完成租约/看板/操作台账迭代与真实浏览器验收。所有测试与证明脚本在每轮迭代后实际复跑。完整披露（包括"人工必须自己做什么"）见 [COLLAB.md](COLLAB.md)。
