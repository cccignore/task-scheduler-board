# 协作与 AI 使用说明

## 实际成员与贡献

| 成员 | 姓名 / 联系方式 | 实质贡献 | 实际耗时 |
| --- | --- | --- | --- |
| A | 陈一诺 / 联系方式未提供 | 单人承担题目选择、需求确认、项目提交与最终责任；开发实现主要由 Codex 辅助完成 | 本次 Codex 辅助开发、审查与验证约 1.5 小时；人工投入未单独计时 |
| B | 无（实际为单人队伍） | 不适用 | 不适用 |

题目原文要求两人组队；本次实际为单人作答，因此不满足该组织形式要求。本文如实披露，不虚构第二名成员、第二作者或协作提交。

## 实际协作过程

- 分工与交接：陈一诺提出完整交付目标并承担提交责任；OpenAI Codex 负责需求拆解、架构设计、代码实现、测试、浏览器验收、对抗审阅和文档整理。
- 人工成员间分歧：无，因为没有第二名人工成员。
- 审阅中发现的问题：独立 Codex 审阅发现了 Worker ID 可冒用、任务读取可能撕裂、旧版 pip 无法 editable 安装，以及证明脚本断言不够严格等问题。
- 最终处理：增加服务端 claim token、SQLite 一致性读事务、旧 pip 可用的非 editable 安装流程，并强化真实多进程/多连接证明与失败清理测试。
- 两人共同验证：不适用。验证由多个独立 Codex 审阅任务及自动化测试完成。

## AI 使用记录

本项目使用 OpenAI Codex 辅助完成题目分析、后端和前端实现、测试设计、证明脚本、代码审阅及交付文档。AI 不作为人工共同作者；陈一诺作为唯一提交者，应在提交前阅读全部代码，并对设计选择、代码正确性和答辩解释负责。

第二轮迭代由 Claude 辅助完成，内容：任务租约与崩溃重领（认领授租、上报续租、过期回收 + token 轮换、断点续跑）、SQLITE_BUSY 映射为 503、认领事务瘦身、矛盾重复上报留痕日志、`scripts/run_worker.py` 真实 worker 演示脚本、参数语义补测（嵌套空串字面值、随机链对照独立 oracle）、租约测试套件，以及看板前端重构（五列流水线看板、任务详情弹层、实时事件流、幂等验证可视化）。全部测试与两个多进程证明脚本在迭代后复跑通过。

第三轮迭代由 Claude 辅助完成，目标是"页面即完整演示，不需要命令行"：新增跨进程 `operation_logs` 操作台账（认领/开工/上报/重复忽略/矛盾告警/租约回收全部落库并在页面展示）、页面托管真实子进程模拟工人（`app/worker_manager.py`，含失败率选项与一键停止）、两个多进程证明脚本的一键运行 API 与结果面板、一键清空重置，任务详情内补执行日志台账表格；首屏文案改为白话比喻。新增 `DESIGN.md` 完整设计文档（题目对照、架构、逐需求设计与取舍、测试体系、已知局限）。迭代后全量测试与证明复跑通过，并经真实浏览器验收。

用户提供的 Gemini API 凭证与题目一无关，未被使用、打印或写入仓库。

## 已执行验证与人工确认边界

已由 Codex 在 Python 3.9.6 / SQLite 3.54.0 / macOS arm64 环境实际执行：

```bash
python3 -m pip install '.[dev]'
python3 -m pytest
python3 scripts/run_concurrency_proof.py
python3 scripts/run_idempotency_proof.py
```

- 验证日期：2026-08-17 至 2026-08-18
- 自动验证执行者：OpenAI Codex
- 结果：12 项测试通过；8 个真实进程、40 轮、320 次认领调用无重复；5 个真实进程重复上报最终仅 1 条日志；真实 Chromium 验证自动轮询、精确 `1 inserted + 4 no-op`、日志台账 1 行及下一 Step 自动运行均通过。
- 人工确认边界：陈一诺尚未在本次对话中声明已亲自逐文件复核或复跑命令，因此不作该项虚假声明；提交前应本人完成并确保能解释关键事务和测试代码。
