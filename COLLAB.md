# 协作与 AI 使用说明

## 参赛形式与成员

| 成员 | 姓名 / 联系方式                                 | 实质贡献                                                                                                     |
| ---- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| A    | 陈一诺 / 1063919295@qq.com（GitHub: cccignore） | 单人承担题目选择、需求确认、方向决策、验收与提交，对全部交付内容负最终责任；代码实现与测试由 AI 结对辅助完成 |

**关于组队要求的说明**：题目要求两人一组；本次实际为单人作答，本仓库如实披露这一点，不虚构第二名成员、第二作者或伪造协作提交历史。是否接受单人形式以题方答复为准；若已另行沟通获得许可，将在答辩时说明。

## 开发过程（三轮迭代）

**第一轮（Codex 辅助，约 0.5 小时）**：需求拆解、架构设计、核心实现（参数合并 / 事务认领 / 幂等日志 / 初版看板）、真实多进程证明脚本与测试。独立的 Codex 对抗审阅发现并修复了：Worker ID 可冒用（引入服务端 claim token）、任务读取可能撕裂（一致性读事务）、旧版 pip 安装问题、证明脚本断言不够严格。

**第二轮（Claude 辅助，约 0.5 小时）**：任务租约与恢复机制、SQLITE_BUSY→503、认领事务瘦身、矛盾上报留痕、`scripts/run_worker.py`、参数语义补测（嵌套空串字面值、200 组随机链对照独立 oracle）、看板重构为五列流水线 + 详情弹层。

**第三轮（Claude 辅助，约 0.5小时）**："页面即完整演示"——跨进程 `operation_logs` 操作台账、页面托管真实子进程模拟工人、证明一键运行、清空重置、`DESIGN.md` 完整设计文档；并根据一次独立外部代码审查完成关键修正：**将"自动回收 running 任务"收窄为"只回收未开工任务 + 已开工任务仅手动重派"**——原设计中凭证轮换只能挡住旧 worker 的数据库写入，挡不住其已执行的外部副作用，存在同一 Step 被真实执行两遍的风险；修正后回收安全性可严格论证（详见 DESIGN.md §4.3）。同轮修复了 `/api/demo` 创建与认领的非原子窗口。

人工投入主要在需求确认、方向决策、逐轮验收，未单独精确计时；AI 辅助时长如上，累计约 1.5 小时。

## AI 使用记录

- 工具：OpenAI Codex（第一轮实现与对抗审阅）、Claude（第二、三轮迭代与真实浏览器验收）、一次独立的外部 AI 代码审查（发现租约重叠执行等问题，均已修复或按建议收窄设计）。

## 已执行的自动化验证（最终代码状态）

由 AI 在 Python 3.9.6 / SQLite 3.54 / macOS arm64 环境实际执行并通过：

- `python3 -m pytest`：**23 项测试通过**（参数语义 7 项、状态机与 API 4 项、租约与手动重派 5 项、操作台账/托管 worker/证明/重置 4 项、多进程证明纳入 3 项）；
- `scripts/run_concurrency_proof.py`：8 真实 spawn 进程 × 40 轮 = 320 次同时认领，重复 0 次；
- `scripts/run_idempotency_proof.py`：5 真实进程同报同一 Step，1 写入 + 4 no-op，双向矛盾覆盖均无效；
- 真实 Chromium 浏览器端到端：页面造任务 → 托管工人抢单执行 → ×5 幂等验证（1 inserted + 4 no-op + 账本 1 行）→ 页面一键复跑两个证明全部 PASS；
- 实跑输出保存于 `evidence/`（与最终提交代码同版本重新生成）。
